name: Simple Deploy (Git Pull)

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger server update
        run: |
          echo "📢 서버에서 git pull을 실행하세요"
          echo "또는 서버에서 webhook을 설정하세요"

